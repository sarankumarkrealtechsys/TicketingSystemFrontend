import React from "react";
import { CreateTicket } from "@/features/ticket-management";

export const CreateTicketPage: React.FC = () => {
  return <CreateTicket />;
};

export default CreateTicketPage;
